## Packages
framer-motion | Complex animations for card flips and transitions
canvas-confetti | Celebration effects when a topic is selected
@types/canvas-confetti | TypeScript types for confetti
clsx | Utility for conditional class names (often paired with tailwind-merge)

## Notes
Polling interval of 1s used for Topic list to simulate real-time updates for all clients.
Admin route is hardcoded to /ithiel as requested.
Card flip animations use CSS 3D transforms via Framer Motion.
